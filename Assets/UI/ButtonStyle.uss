.button-wrapper {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    align-self: flex-end;
    -unity-font-style: bold;
}

.unity-button {
    background-color: rgba(0, 0, 0, 0);
    color: white;
    width: 100%;
}

.level-image {
    margin: 10px;
}
 
